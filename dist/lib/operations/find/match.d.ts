import { Delegate, Item } from '../../delegate';
import { Delegates } from '../../prismock';
import { FindWhereArgs } from '../../types';
export declare const matchMultiple: (item: Item, where: FindWhereArgs, current: Delegate, delegates: Delegates) => boolean;
